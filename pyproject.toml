[project]
name = "mad-hack"
version = "0.1.0"
description = "Treasury AI — robo-advisor for European startups, multi-agent on Orca + Alpaca"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "alpaca-py>=0.43.3",
    "anthropic>=0.39.0",
    "orca-platform-sdk-ui",
    "fastapi",
    "uvicorn",
    "langgraph>=0.2.50",
    "langchain-anthropic>=0.3.0",
    "langchain-mcp-adapters>=0.1.0",
    "mcp>=1.0.0",
    "pyyaml>=6.0",
    "pydantic>=2.0",
    "python-dotenv>=1.2.2",
]
